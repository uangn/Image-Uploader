import { RequestHandler } from "express";

export const getIndex: RequestHandler = (req, res, next) => {
  //
};

export const postIndex: RequestHandler = (req, res, next) => {
  //
};

export const getViewUsers: RequestHandler = (req, res, next) => {
  //
};

export const deleteDeleteUser: RequestHandler = (req, res, next) => {
  //
};

export const deleteImage: RequestHandler = (req, res, next) => {
  //
};
